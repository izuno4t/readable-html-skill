---
name: readable-html
description: Skill for producing a readable, well-structured single-file HTML document. Use when delivering content to a reader matters — specs, implementation plans, PR writeups, code explainers, status reports, postmortems, architecture docs, handoff materials, and similar. Leverages HTML-specific expressiveness (tables, SVG diagrams, code blocks, details/summary, side-by-side comparison) while keeping semantics and accessibility grounded in the Digital Agency Design System (DADS) and WCAG 2.2. Ships no design templates; focuses on content structure and readability. 日本語トリガー:「HTMLにして」「HTMLページを作って」「.htmlで書いて」「ドキュメントをHTML化」「設計書をHTMLに」「HTMLで解説して」「読みやすいHTMLを」「単一ファイルHTMLで」「HTMLレポート」「HTMLエクスプレイナー」「PR解説HTML」など、.html を成果物として生成する場合に必ず使用する。English triggers: create an HTML page/file/doc, convert to HTML, render as HTML, HTML writeup/explainer/report, single-file HTML, readable HTML document. Input format is unconstrained (source code, Markdown, design docs, conversation).
allowed-tools: Read, Write, Edit, MultiEdit, Bash, Glob, Grep
---
